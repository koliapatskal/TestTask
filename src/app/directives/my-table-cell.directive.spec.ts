import { MyTableCellDirective } from './my-table-cell.directive';

describe('MyTableCellDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTableCellDirective();
    expect(directive).toBeTruthy();
  });
});
