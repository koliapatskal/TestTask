import { QDirective } from './q.directive';

describe('QDirective', () => {
  it('should create an instance', () => {
    const directive = new QDirective();
    expect(directive).toBeTruthy();
  });
});
