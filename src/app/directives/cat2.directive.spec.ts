import { Cat2Directive } from './cat2.directive';

describe('Cat2Directive', () => {
  it('should create an instance', () => {
    const directive = new Cat2Directive();
    expect(directive).toBeTruthy();
  });
});
