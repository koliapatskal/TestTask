import { StretchSortCellDirective } from './stretch-sort-cell.directive';

describe('StretchSortCellDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchSortCellDirective();
    expect(directive).toBeTruthy();
  });
});
