import { StretchCellDirective } from './stretch-cell.directive';

describe('StretchCellDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchCellDirective();
    expect(directive).toBeTruthy();
  });
});
