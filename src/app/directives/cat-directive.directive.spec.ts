import { CatDirectiveDirective } from './cat-directive.directive';

describe('CatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
